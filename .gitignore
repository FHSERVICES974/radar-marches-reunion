# Secrets — NE JAMAIS committer
.env

# Python
__pycache__/
*.pyc
venv/
.venv/

# Sorties de veille (proposition lisible — reste locale)
proposition_MAJ_*.md

# data/pending/ EST versionné : c'est le canal qui fait remonter les propositions
# du Mac vers la page de validation /admin sur Replit (via GitHub + webhook /sync).
# Seuls les fichiers de statut intermédiaires restent locaux.
data/pending/status_*.json

# Logs & sauvegardes locales
*.log
data/backups/

# Données runtime (générées en production — ne jamais committer)
data/chat_questions.jsonl
data/theme_analysis.json
data/clicks.jsonl
data/traffic.json
# data/pending_decisions.json est désormais VERSIONNÉ : committé/pushé après
# chaque décision pour survivre aux resets de la VM de production.

# macOS
.DS_Store

# Sorties ingestion documents
proposition_docs_*.md
ingest.log
data/inbox_docs/
data/inbox_mobile.txt
data/inbox_mobile_export.txt
data/inbox_mobile_archive/
# data/pending_completions.json est désormais VERSIONNÉ : committé/pushé après
# chaque complétion IA réussie pour survivre aux resets de la VM de production.

# Logo source haute résolution — seul assets/logo_radar_marches.png (optimisé) est committé
assets/logoradar_source.png

# Documents de travail internes — dépôt PUBLIC, ne jamais committer.
# JOURNAL.md nomme des utilisateurs réels (données personnelles) ; les briefs et
# l'inventaire contiennent la stratégie et des contacts. Ils vivent en local,
# partagés entre François, Claude Code et Cowork sur la machine.
JOURNAL.md
briefs/
INVENTAIRE_SOURCES.html
PLAN_COMPTES_ET_AVIS.html
data/last_report_ok.json

# Sauvegardes horodatées : jamais dans le dépôt (public).
*.bak-*
*.bak2-*
