# Secrets — NE JAMAIS committer
.env

# Python
__pycache__/
*.pyc
venv/
.venv/

# Sorties de veille (proposition lisible — reste locale)
proposition_MAJ_*.md

# data/pending/ EST versionné : c'est le canal qui fait remonter les propositions
# du Mac vers la page de validation /admin sur Replit (via GitHub + webhook /sync).
# Seuls les fichiers de statut intermédiaires restent locaux.
data/pending/status_*.json

# Logs & sauvegardes locales
*.log
data/backups/

# Données runtime (générées en production — ne jamais committer)
data/chat_questions.jsonl
data/theme_analysis.json
data/clicks.jsonl
data/traffic.json

# macOS
.DS_Store

# Sorties ingestion documents
proposition_docs_*.md
ingest.log
data/inbox_docs/
data/inbox_mobile.txt
data/inbox_mobile_export.txt
data/inbox_mobile_archive/
